# 合作开发指南

本文档说明如何让其他开发者一起修改 DSH-Hotplug-Hub 仓库。

## 一、添加合作者（Collaborators）

1. 打开仓库：https://github.com/ARFCON/dsh-hotplug-hub
2. 进入 **Settings**
3. 左侧点击 **Collaborators**（或 **Manage access**）
4. 点击 **Add people**
5. 输入对方的 GitHub 用户名
6. 角色选择 **Write**
7. 点击 **Add <用户名> to this repository**

对方会收到邀请通知；如果没有收到邮件，可以直接访问：
```
https://github.com/ARFCON/dsh-hotplug-hub/invitations
```
接受邀请后即可修改仓库。

## 二、公开仓库的另一种方式：Fork + Pull Request

如果仓库是 Public，不添加合作者也可以参与：

1. 对方点击仓库右上角 **Fork**
2. 在自己的副本里修改
3. 提交 Pull Request 到原仓库
4. 仓库管理员审核后合并

## 三、权限建议

| 角色 | 权限 | 建议 |
|---|---|---|
| Read | 只读 | 访客 / 围观 |
| Triage | 管理 Issue/PR | 社区维护者 |
| Write | 推送分支、开 PR | 日常合作者 |
| Maintain | 管理仓库设置 | 核心维护者 |
| Admin | 全部权限 | 仓库 Owner |

- 普通合作者给 **Write** 即可
- 不建议随便给 **Admin**

## 四、保护 main 分支（强烈建议）

1. Settings → **Branches** → **Add rule**
2. Branch name pattern：`main`
3. 勾选：
   - `Require a pull request before merging`
   - `Require approvals`（至少 1 人）
   - `Require status checks to pass before merging`
4. 保存

这样所有人（包括 Owner）都不能直接往 `main` 推代码，必须走 Pull Request。

## 五、合作者日常工作流

```bash
# 1. 克隆仓库
git clone https://github.com/ARFCON/dsh-hotplug-hub.git
cd dsh-hotplug-hub

# 2. 从 main 拉新分支
git checkout -b feature/xxx main

# 3. 改代码...

# 4. 提交
git add .
git commit -m "feat: 描述你改了什么"

# 5. 推送分支
git push origin feature/xxx
```

然后在 GitHub 页面发起 Pull Request：

- 目标分支：`main`
- 来源分支：`feature/xxx`
- 写清楚改动内容和验收方式

## 六、本地构建验证（提交前必做）

```bash
# 启动器 CLI 测试
node launcher/index.js assemble example
node launcher/index.js check example
node launcher/index.js heal example

# 重新编译桌面 EXE
pwsh -File release/build-exe.ps1

# 重新编译安装程序
pwsh -File installer/build-installer.ps1
```

## 七、更多规范

- 分支规范、提交信息、代码风格见 `CONTRIBUTING.md`
- Issue / 功能建议模板在 `.github/ISSUE_TEMPLATE/`
- CI 自动检查在 `.github/workflows/ci.yml`