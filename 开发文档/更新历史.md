# Dseam世界（DSH-Hotplug-Hub）更新历史

> 更新公告从 README.md 迁移到本文件（2026-08-20 起）。README 只保留一行入口。

## v0.9.8（正式版）
- 版本号 0.9.7 → 0.9.8（按仓库 Owner 指示统一：APP_VERSION / Setup.exe / 页面品牌与更新链接 / README / package.json）；重新构建 EXE 与安装程序。
- 【AI 装配间】全面审查与体验打磨（多轮静态审查 + 全量测试，全部进程隔离）：
  - 人设化细节：占位符 / 示例芯片 / 欢迎语 / 祝贺语 / 错误话术随人设变化；
  - 连接设置面板重做：入口合并为唯一「⚙ 模型」、Key 直接填写（仅本次会话内存）、「测试连接」按钮、预设端点实测校正（OpenCode = Go credits）；
  - EXE（WebView2）渠道与 standalone 逻辑同构：多轮上下文 + 人设注入 + 面板模型优先 + 页面 Key/端点随请求直传外壳（不落盘）+ 未配置 Key 明确提示；
  - 产物卡 per-card 数据绑定（刷新 / 多卡 / 切人设不错位）；中文输入法组词 Enter 不误发送；面板开合 / 视图切换保留输入、滚动与连接设置；
  - 助理气泡轻量 markdown：先转义再渲染（代码围栏 / 行内代码 / 加粗 / 标题 / 表格降级为文本），链接剥为纯文本；用户消息纯文本（XSS 双向防护）。
- 乱码检查：全量 UTF-8 扫描零替换字符；EXE 内嵌 prototype.html 资源与源码逐字节一致（SHA256）；csc（net48 无 BOM UTF-8）实验性验证编译后中文正常。
- 测试：QA6 49 / QA7 42 / QA8 21（新增极端矩阵：XSS / 损坏存储 / confirm 取消 / 重复导入 / 连按 Enter / 极窄视口 / 特殊字符 / markdown / 二次注入 / 导出）/ QA5b 真实 6 轮对话全链路；本地 CI 等价全量（shared-core 170 / launcher 512 + qa3 全套 / memory 51 / hotplug 150 + 覆盖率 / dseam 6 / 契约 68 / csc / build-exe）全部通过。
- 重新构建 EXE（418304 字节）与安装程序（1217024 字节）；check-before-upload 全通过。
- 自检页根治（v0.9.8 补丁，版本号不变）：「DSH 版本 / 官方 Harness」探测不再把本地 `Programs\DSH Desktop` 下的 **Node.js 壳**（文件版本 24.18.0）误认为官方 DSH Desktop——`FindOfficialHarness` 增加官方结构校验（resources/app 或内置 dsh），`GetDshCoreVersion` 改为真实来源优先级探测（官方 Desktop → 全局 node 安装 @deepseek-ai/dsh → dsh CLI → 不再回退 exe 文件版本）；注入脚本不再把「最新版」同化为 DSH 版本、「最新版本」改 semver 比较（本地领先发布时不误报"可更新"）；启动提示改用 IsNewerVersion（0.9.8 尚未发布时不再弹「发现新版本 v0.9.7」、面板未安装不再弹「可更新」）。新增 `scripts/qa9-selfcheck.mjs`（自检三场景实机验收 17 项，从 Main.cs 提取注入脚本等价产物验证）；重新构建 EXE（420352 字节）/ Setup（1219072 字节）。

- AI 装配间修复（2026-08-22，版本号不变）：LoadApiConfig 以 %LOCALAPPDATA%\DSH-Hotplug-Hub\api-config.json 为基底（修复自定义 provider/baseUrl/model 保存后重启即丢）；页面注入不再暴露真实 API Key（只注入 HAS_SHELL_KEY 布尔）；SyncApiConfigToOfficialDesktop 用 ProviderKeyName(cfg.provider) 写回正确密钥行（不再把所有 provider 的 Key 都写成 DEEPSEEK_API_KEY）；CallLlm 请求补 max_tokens=4096、WebException 时返回 HTTP 状态+正文（≤300 字符，key 打码）；parseHotpackJson 按 1-5 个插件 / 小写 id / 精确 semver 权威校验；AI 产物卡按钮 id 重复改为 class；温度 1→0.7 与日志面板可见性修正；重新构建 EXE（421888 字节）与 Setup（1188352 字节）。
- 自检更新修复（2026-08-22，版本号不变）：重新自检按钮在 WebView 中真正 postMessage('recheck') 并清 GitHub 缓存；「更新 DSH」改为「官方客户端」并打开官方下载页（不再伪造本地 state）；自检行补「下载官方客户端 / 检查更新 / 下载新版本」真实动作按钮；BuildNativeSelfCheckScript 删除注入时机不可靠的行内按钮，改由 renderCheck 渲染后绑定。
- AI 对话温度统一为 0.8（2026-08-22，版本号不变）：桌面端与 standalone 页面生成请求统一使用 0.8，语气更自然、更有人情味；旧 api-config.json 保存的温度值不再覆盖。
- 三插件全内置 + 跨平台发布包（2026-08-22，版本号不变）：修复自检页「内置 Skill/MCP 管理器」更新仍请求 Fishquito7 上游导致更新不了的 bug——InstallOrUpdatePanel 改为直接走 EXE 内嵌 tgz 安装到 PANEL_VERSION；EXE 新增内嵌 `dsh-memory-hub-0.8.0-pre.tgz` 与 `dsh-hub-1.1.6.tgz` 资源（build-exe.ps1 三个 /resource），InstallPluginsToHarness / EnsureDshHub 改为优先从内嵌资源安装、网络仅兜底；新增 scripts/build-release-packages.mjs 产出 6 个发布资产（Windows/Linux/macOS × 安装版/便携版，命名 `DSH-Hotplug-Hub-<os>-x64-<setup|portable>-v0.9.8.<ext>`）；Release 不再单独发布 EXE 与插件 tgz；Linux/macOS 便携版为浏览器打开内置 UI 的壳 + plugins/ 目录，安装版为自解压脚本（Linux .sh 内含 tar.gz，macOS .command 内含 base64 zip）；README 下载清单同步更新。
- 自检页 Harness 自动安装/更新 + 环境自愈（2026-08-22，版本号不变）：自检页新增「一键修复环境」与「自动安装/更新官方客户端」按钮（postMessage installHarness/autoInstallEnv）；官方 DSH Desktop 发布源改为 GitHub myYangyunfan/dsh_desktop（不再指向失效的 deepseek-ai/deepseek-harness），GetHarnessReleaseInfo 从 releases/latest 选 Windows x64 安装版（缺省回退便携版 zip）；InstallOrUpdateHarness 下载并静默安装（NSIS /S，便携版 tar 解压到 %LOCALAPPDATA%ProgramsDSH Desktop）；无 Node 时 EnsureNodeEnvironment 从 nodejs.org 下载最新 LTS 便携 zip 解压到 %LOCALAPPDATA%ProgramsDseamWorld
ode，EnsurePnpmEnvironment 用便携 node 跑 npm-cli.js 安装 pnpm；GetPnpmVersion 增加便携 pnpm.cmd 探测、FindDshCommand 优先使用便携 node.exe；OpenOfficialDownloadPage 指向 myYangyunfan/dsh_desktop；重建 EXE（536576 字节）/ Setup（1303040 字节），check-before-upload 全通过。
- 国内镜像源 + skill/MCP/记忆修复（2026-08-22，版本号不变）：InstallOrUpdateHarness/EnsureNodeEnvironment 下载改为镜像优先（GitHub 资产依次走 ghfast.top/gh-proxy.com/ghproxy.net，失败回退直连；Node 版本列表与 zip 依次走 npmmirror.com 与 nodejs.org），镜像请求不携带 GitHub token；RunDshPanelCli 与 AddSkillsFromSource 改用 GetNodeExe()（便携 Node 优先，不再依赖全局 node）；DeleteSkillFile 补充删除单文件停用态 <id>.md.disabled；重建 EXE（538112 字节）/ Setup（1304576 字节），check-before-upload 全通过。
- OpenCode「Ox Alpha Free」接入（2026-08-22，版本号不变）：AI 装配间 OpenCode 预设模型改为 ox-alpha-free；模型管理提供方列表新增 OpenCode（端点 https://opencode.ai/zen/go/v1）；修复 api-config.json 保存后被官方 DSH settings.yaml 覆盖的问题（应用内保存配置优先）；本地 %LOCALAPPDATA% 写入 OpenCode 配置，重建 EXE 538112 / Setup 1304576；check-before-upload 全通过。

## v0.9.7（正式版）
- 版本号 0.9.6 → 0.9.7；重新构建并发布 EXE 与安装程序。
- 插件市场文案：界面导航 / 搜索框占位 / 空态文案统一由「插件包市场 / 插座包」改为「插件市场 / 插件」（prototype.html + lib/client.js + lib/index.js），与真实 GitHub 插件安装能力对齐。
- 修复插件市场「刷新后 暂无市场数据」：根因为 `loadMarket` 第 1 页去重时复用旧 `marketData`，旧条目把本次搜索结果全部判为已加载而列表被清空；第 1 页（含刷新）现从空集开始去重。
- 市场安装改为真实安装：`installMarketEntry` 生成 npm（name@version）或 GitHub codeload tarball 安装源，经 `addPlugin:` 走 `dsh plugin --profile web add` 写入本地插件目录（~/.dsh/profiles/web/node_modules + profile package.json），完成后在「插件管理」页显示；详情加载中 / 已安装 / 不可导入 三类按钮状态区分，「安装」按钮替换原「导入」文案。
- 市场加载时自动检查本地已装：新增 `installedPluginNames` / `marketEntryInstalled` / `marketInstallSpec`，与 `window.__pluginsData`（插件管理页数据）联动；C# `window.__setPlugins` 注入同时刷新市场页，安装/启停后已装徽标即时反映。
- 重新构建 EXE（302080 字节）与安装程序（1068544 字节）；check-before-upload 全通过。

## v0.9.6（正式版）
- 版本号 0.9.5 → 0.9.6；重新构建并发布 EXE 与安装程序。
- 修复版：删除 MCP 市场假示例（MCP_CATALOG 6 条）与插件中枢演示假包（pack-research/pack-notes，已装状态迁移清空）；市场页不再回退内置示例目录；插件包市场刷新/搜索/加载更多保持当前页（state.currentView 持久化 + 按钮 type=button），不再跳回其他界面。
- 插件包市场渐进式加载（PR #18，wrdqtww）：marketList 改为只返回仓库列表元数据（GitHub 标签搜索结果秒出，不再抓 README/package.json 阻塞整页）；新增 marketDetail 单仓库详情（package.json / hotpack / .dshpack / README 三路并行对比，README 提取介绍与安装方法，生成可导入 hotpack manifest）。
- 市场详情独立缓存 market-detail-cache.json（按 repo@ref 缓存，最多 400 条，命中秒回）；客户端并发逐卡替换（谁先返回谁先展示，不再等整页全部结算）。
- 移除仓库独立存活探测 probeRepo（raw.githubusercontent.com CDN 对死仓库快速返回 200(redirect)/404 不再挂起），文件抓取超时 8000ms→5000ms、单仓库预算 12000ms→8000ms、详情并发 4→6。
- Remote 服务 dshHotplug 方法 8→9（新增 marketDetail），lib/index.js + lib/typert.js + lib/client.js 三处同步；重建 EXE 与分发包。

## v0.9.5（正式版）
- 版本号 0.9.4 → 0.9.5；重新构建并发布 EXE 与安装程序。
- 贡献者 YisfL：launcher 六边形六层重构（app/domain/contracts/infra/ports 分层替换 core.js/index.js）；三平台 CI 矩阵（Windows/macOS/Linux）；QA3/QA4 测试补全（76+ 用例 + 真实环境 13 项）；修复 resolve 预发布 pin 失效、github win32 cmd 包装缺失、POSIX 路径断言跨平台化。
- 贡献者 hzhz314159：新增《开发文档/全局记忆-Spec.md》（14 章完整 Spec）；dsh-memory-hub 对齐 0.8.0-pre 补测（test/webapi.test.mjs +3、types.d.ts +4）；scripts git 自动探测（新增 scripts/_git.ps1，sync-repo/check-recent/check-before-upload 不再写死 git.exe 路径）。
- 贡献者 wrdqtww：uninstaller/dsh-desktop 卸载器升级 v1.2.0 模块化多桌面端版本；开发文档3 补充卸载器升级步骤。
## 脚本 git 自动探测（开发中，2026-08-21）
- `scripts/_git.ps1`（新）统一探测 git：`GIT` 环境变量 > PATH > 常见安装路径；缺 git 时报清晰错误。
- `scripts/sync-repo.ps1` / `check-recent.ps1` / `check-before-upload.ps1` 去掉写死的 `C:\Program Files\Git\cmd\git.exe`，dot-source helper 自动解析。
- 实测（本机无系统 git，`$env:GIT` 指向便携 MinGit）：三个脚本全部通过；`check-before-upload` 全流程同步+启动器测试+语法+diff 通过。

## 全局记忆功能 Spec + 测试补充（开发中，2026-08-21）
- 新增《开发文档/全局记忆-Spec.md》：14 章完整 Spec（需求 FR-FD + 非功能 NFR + 架构分层 + 数据模型 + 接口契约 + 设计决策 Q1-Q8/S1-S3 + 里程碑验收 + 安全红线 + 测试策略）。
- dsh-memory-hub 对齐已发布 `0.8.0-pre`（memory.update 工具 / updateDirect·removeDirect / 固定提示行 / GUI 编辑删除），仓库源码与发布物逐字节一致。
- `test/webapi.test.mjs` 新增 3 测试、`types.d.ts` 补 updateDirect/removeDirect 类型；单测 43/43、e2e 10/10、运行时注入通过。

## v0.9.4（正式版）补丁 2（2026-08-20）
- `uninstaller/dsh-desktop` 的 DSH 桌面端卸载器升级为 v1.2.0 模块化版本。
- 源码拆分为 6 个文件：`DSH_Desktop_Uninstaller.cs`（入口/CLI/流水线/日志）、`DSH_Desktop_Uninstaller.Core.cs`（变体档案/名称匹配/保留项模型/日志服务/纯函数）、`DSH_Desktop_Uninstaller.Detection.cs`（安装目录/进程/注册表/变体识别）、`DSH_Desktop_Uninstaller.Cleanup.cs`（进程/文件/快捷方式/注册表/PATH/Run 清理）、`DSH_Desktop_Uninstaller.Retention.cs`（预设/插件/skills 检测与保留/用户数据清理）、`DSH_Desktop_Uninstaller.Gui.cs`（确认弹窗/可滚动保留项列表/进度窗口）。
- 删除无引用的 `DshRetentionContract.cs` 及仅开发用的 `make-uninstaller-icon.ps1`、图标预览图；构建脚本新增 `System.Management.dll` 引用（WMI 进程路径回退需要）。
- 新增保留 skills 选项（`/KeepSkills[=名称1,名称2]`）、手动指定安装目录 `/InstallDir`、预演 `/DryRun`、帮助 `/help`、日志路径 `/Log`。
- 识别到具体仓库后删除逻辑自动收窄到该变体，避免误删其他 DSH 变体；同步更新两份 README 与使用说明。

## v0.9.4（正式版）
- 版本号 0.9.3 → 0.9.4。
- 包含插件启停修复：与 DSH Desktop 官方一致的 cordis.patch.yml 顶层 disabled 条目手术 + loader id 自动解析。
- 重新构建并发布 DSH-Hotplug-Hub.exe 与 DSH-Hotplug-Hub-Setup.exe。
## v0.9.3（正式版）
- dsh-hub 内置到软件本体：启动时自动检查并安装 ARFCON/dsh-hub-DSH（插件仓库保持不变，更新始终从该仓库 main 分支获取）。
- 新增「插件管理」页（导航位于 Skill 管理与自检更新之间）：列出 profile 全部插件依赖，支持添加 / 删除 / 启用 / 关闭 / 检查更新 / 一键更新。
- 插件启停真实生效：统一在 profile cordis.patch.yml 写/删顶层 `disabled: true` 覆盖条目（与 DSH Desktop 设置页「插件」栏同一套手术），loader id 按插件包 cordis.patch.yml 解析（dshmarket→dsh-market、dsh-memory-hub→memory-hub 等）。
- 修复 InstallPluginsToHarness 早退 bug（memory-hub 已最新时不再跳过内置 dseam-skillmcp / dsh-hub 安装）。
- dsh-hub 最新版本查询改用 GitHub Contents API（raw.githubusercontent 在本机网络不可达），带缓存，避免每次刷新都打 API。
- 版本号更新为 v0.9.3（软件本体）；dsh-memory-hub 与内置 dseam-skillmcp 保持 0.8.0-pre。
- arch-optimize 检查：vuln-scan（secret/mcp/config/dep）全部 0 风险。

## v0.9.3（正式版）补丁 1（2026-08-20）
- 修复插件启用 / 关闭不生效：此前按 dsh.profile.bundles / insert 块增删的方式与 DSH Desktop 实际加载机制不一致（bundle 会被启动对账重新加回，非 bundle 关闭后又被 companion sync 重新 insert）。
- 现在完全对齐 DSH Desktop 官方插件管理：启停只改 profile cordis.patch.yml 顶层 `disabled: true` 条目；关闭时同时把该 id 从 insert 块内移除，避免同 id 双登记。
- loader id 自动解析：先读插件包自带 cordis.patch.yml 的 insert id（如 dshmarket→dsh-market、dsh-memory-hub→memory-hub、dsh-ui-guard→ui-guard），非 bundle 插件再从 profile patch 的 insert 块按包名反查（如 dsh-hub），找不到才用净化名兜底。
- 插件列表的「已启用 / 已停用」状态同步改为按顶层 disabled 条目判定，与官方插件管理页一致。
- 未改版本号（仍为 v0.9.3）。

## v0.9.1（正式版）
- 修复固定 Skill 文件夹的「已安装 / 未安装」判断：改为按 SKILL.md frontmatter 的 `name` 匹配 `~/.dsh/skills` 中的安装 id（此前用目录名匹配，arch-optimize-main 这类目录会被误判为未安装）。
- 固定 Skill 文件夹扫描结果按安装 id 去重，避免同一技能重复显示。
- 无 CLI 兜底的 `CopySkillBundleToInstalled` 同步改为按 frontmatter name 安装。
## v0.9.0（正式版）
- v0.9.0-pre 全部内容转正：Skill 固定文件夹扫描安装（可勾选批量装）、skill/mcp 开关真实走 dseam-skillmcp CLI enable/disable、memory-hub 自动安装按版本 tag 解析资产。
- 新增安装程序 `DSH-Hotplug-Hub-Setup.exe`（图形界面一键安装 + `--silent` / `--dir` 静默安装），发布到 Release。
- README 重写为完整项目介绍（功能、安装、目录、开发检查）。
## v0.9.0-pre（先行版）
- Skill 安装改为从固定文件夹读取：默认扫描 `%APPDATA%\reasonix\skills`（可用环境变量 `DSH_SKILL_SOURCE_DIR` 覆盖），递归发现全部 SKILL.md，界面勾选后通过内置 dseam-skillmcp 的 `skill add <目录>` 安装。
- Skill 开关修复：界面开关现在真实调用 `skill enable/disable <name>`（CLI 已验证启停并恢复），不再只改本地预览状态。
- MCP 开关复核：`mcp enable/disable` 与 `mcp test` 已实测正常（godot-mcp streamable-http 启停后恢复）。
- 版本号更新为 v0.9.0-pre（软件本体）；dsh-memory-hub 与内置 dseam-skillmcp 保持 0.8.0-pre。
- memory-hub 自动安装改为解析 `releases/tags/v0.9.0-pre` 资产，修复 prerelease 下 `releases/latest` 会误装旧版 dsh-memory-hub 0.2.1 的问题。
- 新增安装程序 `DSH-Hotplug-Hub-Setup.exe`（内嵌主程序与 WebView2 运行 DLL，图形界面一键安装；支持 `--silent` / `--dir <path>` 静默安装），并发布到 Release v0.9.0-pre。
## v0.8.0-pre（先行版）补丁 1（2026-08-20）
- 单实例进程：EXE 启动用命名 Mutex 保证同用户会话只能存在一个进程，重复启动只提示不重复开启。
- 隐私修复：移除 release/src/Main.cs 中硬编码的本地用户路径（FindOfficialHarness 候选列表），改为全部用 Environment.SpecialFolder 动态解析。
- dsh-hub 插件 1.1.6：设置页左侧边栏自动改为可上下滚动（插件多时不再截断），已同步发布到 ARFCON/dsh-hub-DSH。
- arch-optimize 检查：vuln-scan（secret/config/mcp/dep）全部 0 风险；TODO/占位符/密钥模式扫描无新增问题；空 catch 已全部有说明。

## v0.8.0-pre（先行版）追加
- Skill/MCP 管理器内置：dseam-skillmcp（原开源 dsh-skill-mcp-panel v2.0.1 改名适配，MIT License），源码内置到仓库 vendor/dseam-skillmcp，打包 dseam-skillmcp-0.8.0-pre.tgz 作为 EXE 内嵌资源，启动时自动安装到 ~/.dsh/profiles/web；旧 dsh-skill-mcp-panel 已从本地 profile 卸载，受管 MCP 块标记迁移为 dseam-skillmcp。
- 自检页「官方 Skill/extension 面板」改为「内置 Skill/MCP 管理器」（dseam-skillmcp，随 EXE 自动安装）。

## v0.8.0-pre（先行版）
- 后台常驻：关闭主窗口隐藏到系统托盘，后台进程继续运行（托盘可恢复/退出）。
- MCP 管理修复：统一读写 dsh-skill-mcp-panel/dseam-skillmcp 受管块，支持 STDIO / streamable-http，新增、删除、启停、测试全部走面板 CLI。
- 记忆中枢：支持真实记忆的编辑与删除（直接写入 ~/.dsh/memory-hub），界面按 make-ui-not-ai 重构。
- dsh-memory-hub 升级到 0.8.0-pre：新增 memory.update 工具、插件 GUI 正常配色并支持编辑/删除、重要信息主动提醒 AI 记忆的固定提示。
- 代码质量：空 catch 全部加说明、长函数拆分、C# 后台安装不阻塞 UI。
### v0.2.0（历史）
- 修复 Skill/MCP 面板与 memory-hub 不挂载的问题：插件安装统一走官方 `dsh plugin --profile web add <tgz>`，自动写入 `package.json` 依赖与 `dsh.profile.bundles`，不再手写 `cordis.patch.yml` 插入块。
- 修复记忆中枢在 WebView 中拿不到真实数据的问题：启动时注入 `window.__setMemory` 并自动拉取 `~/.dsh/memory-hub`，移除 3 个演示用假记忆包。
- EXE 启动时后台自动安装/更新 `dsh-memory-hub`（从 GitHub Release 解析 tgz 资产，失败写日志不阻塞启动）。
- 跨平台安装脚本 `scripts/install-plugins.mjs` 同步改为 `dsh plugin add` 方式。
### v0.1.9（历史）
- 修复更新程序错误：dsh CLI 探测不再把 `cmd.exe` 版本横幅误判为 dsh；安装前自动写 `~/.dsh/profiles/web/.npmrc` 关闭严格 SSL，解决 GitHub Release 下载报 `UNABLE_TO_VERIFY_LEAF_SIGNATURE` 的问题。
### v0.1.8（历史）
- 官方面板插件 `dsh-skill-mcp-panel` 自动安装/更新：自检页一键安装、打开时检查、修改后复查。
- 自检页新增「官方 Skill/MCP 面板」状态与安装/更新按钮，最新版本从 GitHub Release 自动解析。
### v0.1.7（历史）
- 记忆中枢真实化：读取/写入 `~/.dsh/memory-hub`，UI 按规范展示真实记忆。
- 功能审查：移除插件包市场示例、默认 Skill/MCP；审查清单写入开发踩坑记录。
- EXE 启动自动安装/注册 memory-hub 到本地 DeepSeek Harness。
- 新增跨平台自动安装脚本 scripts/install-plugins.mjs，适配 Windows/macOS/Linux。
### v0.1.6（历史）
- AI 协作：新增「必须逐条汇报遵守流程」规则，每次任务开始/结束都强制自检。
- 任务审查：2026-08-19 已执行并通过。
- 任务审查：2026-08-19 Skill/MCP 接入官方 Harness 已通过。
- 任务审查：改为 AI 自动执行，不再询问用户；2026-08-19 自动审查通过。
- 确认官方 DSH Desktop 内置 `dsh-skill-filesystem` 与 `dsh-mcp-client`；Skill 已写成官方 frontmatter 格式，MCP 已注册到 web profile。
- 移除默认 MCP 自动填充/注册，只保留用户手动添加的真实 MCP；自动审查通过。
- 记忆中枢真实对接：`remember-doc.ps1` 现在同时写入 `~/.dsh/memory/memories.jsonl` 和 memory-hub 提案队列（`~/.dsh/memory-hub/`），走插件协议。
- EXE 启动时自动安装/注册 `dsh-memory-hub` 到本地 DeepSeek Harness（复制到 `~/.dsh/plugin-src/` 并启用 web profile 注册）。
- 精简 Skill/MCP 市场，只保留真实可用的官方条目；自动审查通过。
- 真实接入官方 Harness：
  - Skill 按官方 `skill-filesystem` 格式写入 `~/.dsh/skills/*.md`（YAML frontmatter）
  - MCP 保存时自动注册 `@deepseek-ai/dsh-mcp-client` 到 web profile 的 `cordis.patch.yml`
- 打开客户端时自动检查 GitHub 最新版本，发现新版本会提示。
- 修改/同步仓库时自动检查最新版本，有新版本先更新再修改。
- 每次更新完成后自动 git push，并同步更新 README 更新公告。
### v0.1.5 修复（历史）
- 模型配置：提供方列表只读取官方配置中真实存在的提供方，不再显示假的 OpenAI/通义/智谱/自定义。
- 每个提供方的 Base URL、模型列表、默认模型均按官方 `~/.dsh/settings.yaml` 真实解析。
- 修复模型不全、与官方配置对不上的问题。

- 新增 Linux 跨平台支持：`scripts/linux/`，launcher 支持 `dsh` CLI。
- 新增自更新检查：自检页显示「本程序版本 / 最新版本」，可一键检查更新与下载新版本。
- macOS / Linux / Windows 三端支持。


- 新增 macOS 跨平台支持：`launcher` 可在 macOS 运行，提供 `.app` 壳脚本。
- 修复 Windows 自检 pnpm 未检测到。
- 新增跨平台规范：`开发文档/规范/跨平台支持规范.md`。
- 团队协作/AI 协作文档同步更新。
