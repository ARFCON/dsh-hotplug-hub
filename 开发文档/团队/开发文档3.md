# 开发文档3

> 负责人：wrdqtww
> 职责：负责 DSH-Hotplug-Hub 开发文档3 的编写与维护。

## 我负责的内容
- [x] DSH-Hotplug-Hub 卸载程序（uninstaller/）
- [x] 卸载程序构建脚本与图标嵌入
- [x] 卸载程序使用说明
- [x] 开发文档3 维护（团队目录）

## 任务背景

DSH-Hotplug-Hub 已有桌面安装程序 `installer/Setup.exe`，但存在核心短板：

- 安装器默认安装到 `C:\DSH-Hotplug-Hub`，不写注册表卸载项。
- 用户无法从“设置/控制面板”正常卸载，缺少完整卸载程序。

因此本任务为 DSH-Hotplug-Hub 制作一套完整、可靠、有交互式保留选项的卸载程序。

## 实现目标

1. 提供 GUI 卸载确认弹窗：卸载模式 + 可选保留项 + 二次确认。
2. 支持静默卸载 `/S` 与命令行保留/模式参数，适配批量部署和后续安装器集成。
3. 完整清理范围：
   - 运行中的 `DSH-Hotplug-Hub.exe` 进程（优雅关闭 + 超时强制）。
   - 桌面/开始菜单快捷方式（当前用户 + 公共用户）。
   - 安装目录。
   - 用户数据：`%LOCALAPPDATA%\DSH-Hotplug-Hub`、`%TEMP%\dsh-hotplug-hub-webview2`。
   - DSH 共享数据：`%USERPROFILE%\.dsh\hotplug-store`、`%USERPROFILE%\.dsh\hotplug-hub`。
   - 注册表残留：`HKCU\Software\DSH-Hotplug-Hub`、HKCU 卸载键。
4. 不触碰 DSH 官方 Harness 数据（`~/.dsh/profiles`、`settings.yaml`、`.credentials.yaml`）。
5. 卸载过程写 `Log.log`，便于排障。
6. 支持一键重建（`uninstaller/build-uninstaller.ps1`），保证可复现产物。

## 技术路线

采用“复用 DSH Desktop 卸载器成熟模式，针对 Hotplug Hub 精简重构”的路线。

### 经验复用（来自 DSH_Desktop_Uninstaller 项目）
- P2 优雅关进程：`CloseMainWindow` 优先 → 等待 3 秒 → 二次强制 → sleep 500ms。
- P7 安全目录删除：重解析点保护（只删链接本身）、清只读属性、文件优先逐个删、
  子目录递归、最终目录删除不吞异常（配合外层 8 次重试 × 800ms）。
- fake-success 修复：删除失败必须向上抛，不能误报“Deleted directory”。
- 二次确认弹窗：卸载前 MessageBox OKCancel + Warning 展示保留摘要。
- GUI 表单：Microsoft YaHei UI，主题色 `Color.FromArgb(14,124,107)` 与安装器一致。
- 日志：`InitializeLog` + `Log` 追加 `Log.log`（UTF-8）。

### 针对 Hotplug Hub 的差异化设计
- 安装器不写注册表 → 安装目录检测改为多路探测：
  当前 exe 目录、常见安装路径、运行中进程目录。
- 保留项精简为三类：应用配置与数据、DSH 共享缓存、临时缓存。
- 共享数据单独成项，避免卸载误伤 DSH 生态其他组件。
- 构建脚本自动补 UTF-8 BOM，确保 csc.exe 正确编译中文字符串。

### 编译链路
- `csc.exe`（.NET Framework v4.0.30319 / 4.8）→ `/target:winexe` → GUI 子系统。
- 引用 `System.Windows.Forms.dll`、`System.Drawing.dll`。
- `embed-icon-in-exe.ps1` 用 Win32 UpdateResource 把 `release\src\app.ico` 嵌入 EXE。

## 更改/新增文件

以下均在仓库根相对路径下：

### 新增
- `uninstaller/Uninstall_Hotplug_Hub.cs` —— 卸载程序完整 C# 源码（约 580 行）
- `uninstaller/Uninstall_Hotplug_Hub.exe` —— 最终二进制（19,968 字节，GUI，已嵌图标）
- `uninstaller/Uninstall_Hotplug_Hub_说明.txt` —— 软件使用说明（交互/CLI/重建方法）
- `uninstaller/build-uninstaller.ps1` —— 一键构建脚本（BOM + 编译 + 嵌图标）
- `uninstaller/embed-icon-in-exe.ps1` —— 图标嵌入脚本（复用 DSH Desktop 方案）
- 开发文档/团队/开发文档3.md —— 本文档

### 修改
- `.gitignore` —— 忽略本地备份目录 `uninstaller/backups/`

### 删除
- `开发文档/开发文档3.txt` —— 旧位置/格式错误，由 `开发文档/团队/开发文档3.md` 取代

## 期望实现效果

1. 交互卸载：
   - 双击 `Uninstall_Hotplug_Hub.exe` → 弹窗：
     - 卸载模式：程序识别卸载 / 默认卸载
     - 可选保留项：应用配置与数据 / DSH 共享缓存 / 临时缓存（默认不保留）
     - 点击“卸载”后 → 二次确认（展示将保留哪些内容）→ 开始卸载。
   - 卸载完成后弹窗提示，详细过程记录于当前目录 `Log.log`。
2. 静默卸载：
   - `Uninstall_Hotplug_Hub.exe /S` —— 全删不弹窗。
   - `/S /KeepAppData` —— 保留 `%LOCALAPPDATA%\DSH-Hotplug-Hub`。
   - `/S /KeepSharedData` —— 保留 `.dsh\hotplug-store` + `.dsh\hotplug-hub`。
   - `/S /KeepTempData` —— 保留 `%TEMP%\dsh-hotplug-hub-webview2`。
   - `/S /KeepAll` —— 保留以上全部。
   - `/S /DetectRunning` / `/S /Default` —— 切换安装目录检测模式。
3. 安全性：不会删除官方 Harness 的 profiles/settings/credentials；删除失败不误报成功。
4. 可复现：构建脚本输出固定产物，便于后续接入 CI 或安装器。

## 本地验证结果

- 冒烟测试：`Uninstall_Hotplug_Hub.exe /S /KeepAll`
  - 退出码 0。
  - 日志六步全部正确：进程清理 → 快捷方式 → 安装目录检测 → 用户数据保留 → 临时数据保留
    → 注册表清理 → 卸载完成。
  - 验证保留项生效（保留应用数据/共享数据/临时数据）。
- 编码验证：通过 IL 字符串转储确认 EXE 内含全部中文界面字符串
  （“DSH-Hotplug-Hub 卸载确认”“可选保留项”“保留应用配置与数据”等）。
- 子系统验证：PE Subsystem=2（GUI）。

## 后续可扩展

1. 安装器集成：`Setup.exe` 在安装时复制 `Uninstall_Hotplug_Hub.exe` 到安装目录，
   并写入 HKCU/HKLM 卸载注册项 + `QuietUninstallString`，使控制面板可卸载。
2. 自动化测试：补充对临时目录清理的安全匹配（参考 DSH Desktop 的 dsh- 前缀过滤）。
3. 按团队规范将本分支提交 PR 到上游。

## 本次更新步骤：DSH Desktop 卸载器升级 v1.2.0（2026-08-20）

目标：把 `uninstaller/dsh-desktop` 从旧单文件版本升级为 v1.2.0 模块化多桌面端版本，并提交 PR 到 `ARFCON/dsh-hotplug-hub`。

### 操作步骤（可复现）

1. **同步产品文件**：从 `F:\DeepSeek\DSH_Desktop_Uninstaller` 复制 6 个 `.cs`、`DSH_Desktop_卸载说明.txt`、`Uninstall_DSH_Desktop.exe`、`Uninstall_DSH_Desktop_icon.ico`、`embed-icon-in-exe.ps1` 到 `uninstaller/dsh-desktop/`。
2. **删除旧文件**：删除 `uninstaller/dsh-desktop/DshRetentionContract.cs`（无引用）、`make-uninstaller-icon.ps1` 与 `Uninstall_DSH_Desktop_icon_preview.png`（仅开发用）。
3. **更新构建脚本**：`uninstaller/dsh-desktop/build-uninstaller.ps1` 引用列表追加 `/r:$fwDir\System.Management.dll`（WMI 进程路径回退需要）；脚本保持“自动编译目录内全部 .cs”。
4. **更新文档**：`uninstaller/README.md`、`uninstaller/dsh-desktop/README.md`、`DSH_Desktop_卸载说明.txt` 同步目录结构与 CLI 表；`开发文档/更新历史.md` 追加补丁 2 小节。
5. **构建**：`powershell -NoProfile -ExecutionPolicy Bypass -File uninstaller\dsh-desktop\build-uninstaller.ps1`，产物 `uninstaller/dsh-desktop/build/Uninstall_DSH_Desktop.exe`（463360 字节）。
6. **冒烟验证**：`Uninstall_DSH_Desktop.exe /DryRun /Log=<临时路径>`，确认退出码 0、日志写入指定路径、变体识别与附加目录列表正确。
7. **提交前检查**：`git diff --check`；对 `build-uninstaller.ps1` 做 PowerShell 语法解析；确认只改了 `uninstaller/` 与 `开发文档/更新历史.md`，不碰 `hotplug-hub`、`installer`、`launcher`。
8. **提交 PR**：分支基于最新 `origin/main`，推送到 fork 后向 `ARFCON/dsh-hotplug-hub` 发 PR（本次为 PR #13）。

### 过程中发现并修复的问题

- `/Log=<路径>` 会被 `InitializeRuntime()` 内的默认日志路径覆盖，导致自定义日志不生效。
- 修复：`Main` 中先 `ParseArgs` → `InitializeRuntime` → 应用 `/Log` 覆盖 → `InitializeLog`。修复后已重新构建并验证 `/Log` 正确写入指定文件。

## 修改记录

每次修改前必须检查目标文件最近是否被修改过：

```bash
git log --oneline -10 -- 开发文档/团队/开发文档3.md
git status --short
```

| 日期 | 修改文件 | 检查结果 | 修改内容 |
|---|---|---|---|
| 2026-08-19 | 开发文档/团队/开发文档3.md | 新建，检查 git log 最近修改人：无历史 | 建立 3 号开发文档，记录卸载程序开发路线/目标/文件/效果 |
| 2026-08-19 | 开发文档/开发文档3.txt | 最近修改人：wrdqtww（PR #1），无冲突 | 移除错误位置/格式文档，改用团队目录 md |
| 2026-08-19 | 开发文档/团队/开发文档3.md | 最近修改人：wrdqtww，无冲突 | 负责人改为 wrdqtww |
| 2026-08-20 | uninstaller/dsh-desktop/ 全部、uninstaller/README.md、开发文档/更新历史.md | git log 最近修改人：ARFCON（48d1aef、958aa05），无冲突 | 升级 DSH Desktop 卸载器到 v1.2.0 模块化版本，补充构建/验证步骤到本文档 |

## AI 使用说明
- 本文档由 AI 协作成员协助维护。
- 修改前运行 `scripts/check-recent.ps1`（本机若脚本 git 路径不可用，可用 `git log` 手工检查）
- 读取后运行 `scripts/remember-doc.ps1`
- 遵守根目录 `AI_AGENTS.md`